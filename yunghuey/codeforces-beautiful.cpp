#include <iostream>
using namespace std;

int main(){
    int tc ;
    cin>> tc;

    while(tc--){
        int no = 0;
        cin >> no;
        int arr[100];
        for (int i = 0; i< no; i++){
            cin >> arr[i];
        }

        for (int a = 0; a < no; a++){
            for(int b = 0; b < no; b++){
                if (a != b){

                }
            }
        }
    }
}
